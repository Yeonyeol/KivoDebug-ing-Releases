# 공개 배포 저장소 작업 규칙

이 저장소는 `Yeonyeol/KivoDebug-ing-Releases`이며 플레이어용 공개 배포 창구다.

- 사용자 요청의 기본 구조는 비공개 개발·백업과 공개 배포의 분리다. 이 저장소에 개발 프로젝트나 개인 백업을 동기화하지 않는다.
- Git에는 안내 문서와 승인된 공개용 자료만 보관한다. 게임 ZIP / APK / 체크섬은 Releases 첨부 파일이다.
- 개발자가 선택한 규모가 큰 업데이트만 공개한다. 커밋, 푸시, 병합, 문서 수정 또는 일반적인 백업 요청을 공개 배포 승인으로 해석하지 않는다.
- 게임 릴리스는 초안을 먼저 준비하고 해당 버전의 명시적인 공개 승인을 확인한 뒤 게시한다. 파일이 없는 릴리스, 미검증 지원 환경, 실제 수행하지 않은 검사 결과를 만들어내지 않는다.
- 다른 저장소의 브랜치, 소스, 원본 에셋, 내부 문서 또는 전체 Git 이력을 가져오지 않는다. 공개용 태그는 이 저장소의 문서 커밋을 대상으로 한다.
- 개인 토큰, 환경 변수, 서명 키, 민감한 로그를 커밋하지 않는다. `.gitignore`만으로 안전성을 보장하지 말고 실제 변경 목록을 검토한다.
- 별도 요청 없이 자동 공개 워크플로, 미러링, 저장소 공개 범위 변경, 강제 푸시, 기존 릴리스·태그 삭제나 바이너리 덮어쓰기를 수행하지 않는다.
- 공개 문서는 한국어로 작성하고 내부 개발 링크를 넣지 않는다. 기존에 통과한 게임 파일은 문서 작업 때문에 다시 빌드하거나 수정하지 않는다.
- 작업 완료 시 문서 변경, 초안 생성, 파일 업로드, 실제 공개 중 어디까지 성공했는지 구별해 보고한다.

세부 절차는 `DISTRIBUTION.md`, 공개용 설명은 `RELEASE_TEMPLATE.md`를 따른다.
